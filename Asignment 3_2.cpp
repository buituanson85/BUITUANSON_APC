#include<stdio.h>

int main(){
	int st,st1,st2,st3,st4;
	    st=4000;
	    st1=(float)(st+st*0.8);
	    st2=(float)(st1+st1*0.8);
	    st3=(float)(st2+st2*0.8);
	    st4=(float)(st3+st3*0.8);
	printf("Tong so tien thu duoc sau 4 nam la:\n %d $",st4);
}
